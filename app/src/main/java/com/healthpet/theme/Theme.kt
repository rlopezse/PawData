package com.healthpet.theme

import androidx.compose.material3.lightColorScheme
import androidx.compose.ui.graphics.Color

val LightColorScheme = lightColorScheme(
    primary = primary500,
    onPrimary = Color.White,
    primaryContainer = primary100,
    onPrimaryContainer = primary700,
    secondary = secondary500,
    onSecondary = Color.White,
    secondaryContainer = secondary100,
    onSecondaryContainer = secondary700,
    tertiary = tertiary500,
    onTertiary = Color.White,
    tertiaryContainer = tertiary100,
    onTertiaryContainer = tertiary700,
    error = errorSemantic500,
    onError = Color.White,
    errorContainer = errorSemantic50,
    onErrorContainer = errorSemantic800,
    background = bgColor,
    onBackground = pureBlack,
    surface = Color.White,
    onSurface = pureBlack,
    surfaceVariant = neutral100,
    onSurfaceVariant = neutral600,
    outline = neutral400,
    outlineVariant = neutral200,
    scrim = pureBlack,
    inverseSurface = neutral800,
    inverseOnSurface = neutral50,
    inversePrimary = primary200,

)